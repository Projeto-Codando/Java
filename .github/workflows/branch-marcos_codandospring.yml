# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions
name: Build and deploy JAR app to Azure Web App - CodandoSpring
on:
  push:
    branches:
      - branch-marcos
  workflow_dispatch:

jobs:

    
        
          
    

        
        Expand All
    
    @@ -21,8 +21,11 @@ jobs:
  
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Java version
        uses: actions/setup-java@v1
        with:
          java-version: '17'

      - name: Verify directory structure
        run: ls -R

      - name: Build with Maven
        run: mvn -f api-codando/pom.xml clean install -e -X

      - name: List files after build
        run: ls -R api-codando/target

    
          
            
    

          
          Expand Down
    
    
  
      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v3
        with:
          name: java-app
          path: 'api-codando/target/*.jar'
  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
    permissions:
      id-token: write #This is required for requesting the JWT
    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v3
        with:
          name: java-app
      
      - name: List downloaded files
        run: ls -R
      - name: Login to Azure
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_538A92308E8E43919A25FAEC79CBE366 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_E67433FE7A8343B680CC6AC5FD9262FA }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_D1CFD73A15CE4E6496DD095BC5D83951 }}
      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'CodandoSpring'
          slot-name: 'Production'
          package: '*.jar'
